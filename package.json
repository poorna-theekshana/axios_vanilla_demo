{
  "name": "axios-demo",
  "version": "1.0.0",
  "description": "A simple demo using Axios to fetch API data",
  "main": "main.js",
  "scripts": {
    "start": "http-server .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.4"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "private": true
}
